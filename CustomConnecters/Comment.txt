Следует рефнуть коннектор следующим образом:
Так как у всех пакетов есть методы:

connection = ПАКЕТ.connect()
connection.close()
connection.cursor()

cursor.execute()
cursor.fetchmany()
cursor.rollback()
execute.close()

Cтоит свести всю "разницу" к выбору
прототипа конектора, реализующего нужные методы, т.к. во всём прочем работа ни чем не отличается.